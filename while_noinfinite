#include <stdio.h>
int main(){
    int i = 1;
    while(i <= 5){
        printf("%d\n",i);
        i++; // operador que impossibilita a máquina de realizar o processo infinitamente
    }
    return 0;
}
